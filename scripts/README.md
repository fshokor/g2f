# Scripts

Reusable Python modules imported by notebooks (data loading, effect
representations, model definitions, diagnostic/orthogonalization utilities,
evaluation helpers). Typed function signatures, minimal comments (why not
what), no print-inside-functions.
